<div class='w-auto p-4'>
	<div class='list-group'>
		<h2 class='px-4 mt-4'>Comments</h2>
		<ul>
			{{#each comments}}
				<div class='list-group-item p-2 mb-2'>
					{{!-- <li>
					Title:
					{{this.postTitle}}
				</li> --}}
					<div class='mb-2'>
						<p>{{this.body}}</p>
						<h6>From: {{this.email}}</h6>
					</div>
				</div>
			{{/each}}
		</ul>
	</div>
	<a href='/post/top' type='button' class='btn btn-primary text-light'>Back</a>
	<a href='/' type='button' class='btn btn-primary text-light'>Home</a>
</div>