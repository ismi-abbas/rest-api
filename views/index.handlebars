<div class='w-auto p-4'>
	<h1>{{appTitle}}</h1>
	{{!-- <ul class='list-group'>
		{{#each post}}
			<li class='list-group-item'>Title: {{this.postTitle}}</li>
			<li>Body: {{this.body}}</li>
			<li>Comments: {{comments}}</li>
		{{/each}} --}}
	</ul>

	<a href='/post/top' type='button' class='btn btn-primary text-light'>See all post</a>
</div>